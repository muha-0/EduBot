// @ts-ignore
const Notification = ({onClick}) => {
    return (
        <div className="notification" onClick={onClick}>
            <p>I'm The AI Assistant For the Faculty of Computing and Information Sciences. <br/> Click on me If you
                needed help with anything! 😊</p>
        </div>
    )
};

export default Notification;
